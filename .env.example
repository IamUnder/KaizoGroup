# Web3Forms — clave pública de acceso (https://web3forms.com), segura para exponer en cliente
PUBLIC_WEB3FORMS_ACCESS_KEY=

# Umami Analytics (cloud.umami.is, plan gratuito) — ID del sitio (deja vacío para desactivar)
PUBLIC_UMAMI_WEBSITE_ID=
# Solo si autoalojas Umami en vez de usar cloud.umami.is — URL de tu script.js propio
PUBLIC_UMAMI_SRC=

# Microsoft Clarity — ID de proyecto (opcional, deja vacío para desactivar el mapa de calor)
PUBLIC_CLARITY_ID=

# Portledger — endpoint público de captura de leads (opcional, deja vacío para desactivar el envío
# a Portledger; el formulario sigue funcionando por email vía Web3Forms de todas formas)
PUBLIC_PORTLEDGER_LEADS_URL=
PUBLIC_PORTLEDGER_LEADS_KEY=

# --- Solo para docker-compose (no las usa `npm run dev`) ---

# Dominio de producción, usado por Traefik/Caddy para enrutar y emitir el certificado TLS
SITE_DOMAIN=kaizogroup.es

# Nombre de la red Docker externa de tu Traefik/PaaS (solo docker-compose.yml, no el standalone)
TRAEFIK_NETWORK=proxy
